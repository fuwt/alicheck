name: Aliyun Signin

on:
  schedule:
   # 每天国际时间 17:20 运行一次, 中国时间 01:20
    - cron: '20 17 * * *'
  workflow_dispatch:
jobs:
  signin:
    name: Aliyun Signin
    runs-on: ubuntu-latest
    steps:
      - uses: ImYrS/aliyun-auto-signin@main
        with:
          REFRESH_TOKENS: ${{ secrets.5d809897b2724d15a434630e7bb9b9fc }}
          PUSH_TYPES: 'ServerChan'
          SERVERCHAN_SEND_KEY: ${{ secrets.SCT188316TPAmttHh0sKfvIdMcdrsBGriq }}
